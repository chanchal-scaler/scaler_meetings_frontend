import CallWidget from '~meetings/widgets/call';
import FullWidget from '~meetings/widgets/full';
import meetingEvents from './events';

import '~meetings/index.scss';

export default {
  events: meetingEvents,
  CallWidget,
  FullWidget,
};
