import React from 'react';

function SelectFilter() {
  return (
    <div>
      This filter will be enabled after we add a react select component to
      our codebase
    </div>
  );
}

export default SelectFilter;
